extends layout

block content
     h1= title
     p Welcome to #{title}
     form(action="/nav", method="POST")
          input(name="search_keyword", type="text", placeholder="Masukkan kata kunci")
          button(type="submit") Submit